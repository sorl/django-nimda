What is it?
===========
A light weight attempt at spicing up the django admin.


Installation
------------
::
    INSTALLED_APPS = (
        ...
        'nimda',
        ...
        'django.contrib.admin',
        ....
    )

